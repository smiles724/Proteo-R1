# global batch size = 16 node x 1 gpu x 4 x 1 = 256
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
dataloader_num_workers: 1

learning_rate: 1e-5
weight_decay: 0.0
warmup_ratio: 0.03
lr_scheduler_type: "cosine"

save_steps: 100
save_total_limit: 2
